kind = "web"
previewPath = "/"
title = "Smart ERP & BI"
version = "1.0.0"
id = "artifacts/smart-erp"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22372

[services.development]
run = "pnpm --filter @workspace/smart-erp run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/smart-erp", "run", "build" ]
publicDir = "artifacts/smart-erp/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22372"
BASE_PATH = "/"
